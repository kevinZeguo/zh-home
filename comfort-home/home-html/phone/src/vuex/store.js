import Vue from 'vue';

import Vuex from 'vuex';
Vue.use(Vuex);

// 数据存储
let state = {
};


// mutations
let mutations = {
    
};


const store = new Vuex.Store({
	state,
	mutations
});

export default store;
